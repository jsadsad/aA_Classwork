import React from "react";

// It will accept the store as a prop, and render routes wrapped by the Provider.

const App = () => (
    <div>
        <h1>Bench BnB</h1>
    </div>
);

export default App;