import React from 'react'

const tabInfo = (props) => {
    const tabArr = props.tabs.map((nums) => {
        // must return in a map!!
        return (
            <li>
                content=
                // key={listing.id}
                // company={listing.company}
                // title={listing.title}
                // type={listing.type}
                // location={listing.location}
            </li>
        );
    });