class View {
  constructor(game, $el) {}

  bindEvents() {}

  makeMove($square) {}

  setupBoard() {
    const $newUl = $("<ul></ul>");
    for (let row=0; row<3; row++){
      for (let col = 0; col < 3; col++){
        
      }
        
    }
  }
}

module.exports = View;
